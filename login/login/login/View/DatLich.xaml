<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="login.View.DatLich"
             Title="Dat Lich Kham">
    <ContentPage.Content>
        <ListView x:Name="List_bacsi" SeparatorVisibility="Default">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ImageCell Text="{Binding ten_bs}" ImageSource="{Binding img_bs}" Detail="{Binding khoa}"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </ContentPage.Content>
</ContentPage>